SmartFit — Improved UI
Files:
- index.html
- styles.css
- app.js

Features:
- Polished, non-basic UI with animations and measurement badges
- Camera with live skeleton + measurement overlay
- Export measurements as JSON
- Mock API for upload/tryon

Run:
- Open in VS Code and use Live Server (camera requires secure origin)
- Toggle mock mode by editing app.js if needed
